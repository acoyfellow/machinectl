{
  "name": "machinectl-worker-relay-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:test": "rm -rf dist && tsc --noEmit false --outDir dist",
    "test": "npm run typecheck && npm run build:test && node --test test/*.test.mjs && rm -rf dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.12.19",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260515.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.92.0"
  }
}
